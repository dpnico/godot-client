name: CI/CD

on:
  push:
  pull_request:

jobs:

  test:

    runs-on: ubuntu-latest

    container:
      image: liphium/godot-mono:4.6.2

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Lint
        run: dotnet format godot-client.sln --verify-no-changes --verbosity diagnostic

      - name: NUnit Test
        run: dotnet test godot-client.Test/godot-client.Test.csproj

      - name: Import Godot
        run: godot --headless --path .
        
      - name: GdUnit Test
        run: bash addons/gdUnit4/runtest.sh